<header>
  <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">ARCADIA parc zoologique</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="{{ path ('app_home') }}">Accueil</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="">Services</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path ('app_habitat') }}">Habitats</a>
          </li>
          <li>
            <a class="nav-link" href="">Contact</a>
          </li>
          {% if is_granted('ROLE_ADMIN')  %}
          <li>
            <a class="nav-link" href="{{ path ('admin') }}">Dashboard</a>
          </li>
          {% endif %}
          {% if is_granted('ROLE_USER')  %}
          <li>
            <a class="nav-link" href="">Employé</a>
          </li>
          {% endif %}
          {% if is_granted('ROLE_VETERINAIRE')  %}
          <li>
            <a class="nav-link" href="">Vétérinaire</a>
          </li>
          {% endif %}
          {% if app.user %}
          <li>
            <a class="nav-link" href="{{ path ('app_logout') }}">Déconnexion</a>
          </li>
          {% else %}
          <li>
            <a class="nav-link" href="{{ path ('app_login') }}">Connexion</a>
          </li>
          {% endif %}
        </ul>
      </div>
    </div>
  </nav>
</header>
