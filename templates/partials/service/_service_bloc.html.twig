<div class="container p-4">
    <article class="bg-secondary text-white rounded">
        <div class="container p-4">
            <h2 class="text-center">{{ zoo.nom }}</h2>
            <div class="row row-col-2 align-items-center">
                <div class="col">
                    <p class="text-justify">{{ zoo.description| raw }}</p>
                </div>
                <div class="col image-card text-white">
                    <img src="{{ vich_uploader_asset(zoo, 'imageFile') }}" class="card-img-top rounded" alt="affiche {{ zoo.nom }}">
                </div>
            </div>
        </div>
    </article>
</div>
